export const metadata = {
  title: "Essential Radio",
  description: "Listen live and get the latest tunes and college news on Essential Radio"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  );
}
